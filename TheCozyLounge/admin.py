from django.contrib import admin
from .models import BookDetails
# Register your models here.
admin.site.register(BookDetails) #this means admin can edit anything in the data base if bookdetails

